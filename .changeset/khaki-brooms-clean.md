---
"@localyze-pluto/components": major
---

Remove the `AlertDialog` component and replace it with the `ConfirmationModal` component which re-uses the `Modal` component to unify the appearance of modals
